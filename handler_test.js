describe('Slack notifier', () => {

  it('should send error message to Slack if SNS message contains `err` attribute')
  it('should send success message to Slack if SNS message does not contain `err` attribute')

})
